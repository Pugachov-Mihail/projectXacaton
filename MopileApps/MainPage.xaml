<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:MopileApps"
             x:Class="MopileApps.MainPage">

    <StackLayout>
        <Label FontSize="Medium" FontAttributes="Bold" Text="{Binding News}" />
        <Label FontSize="Medium" FontAttributes="Bold"  Text="{Binding Title}" />
        <Label FontSize="Medium" FontAttributes="Bold" Text="{Binding Text}" />
        <Button Text="Получить новости" Command="{Binding LoadDataCommand}"  />
    </StackLayout>

</ContentPage>
